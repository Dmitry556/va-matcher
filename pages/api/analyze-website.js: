import OpenAI from 'openai';

export default async function handler(req, res) {
  if (req.method !== 'POST') {
    return res.status(405).json({ message: 'Method not allowed' });
  }

  const { website } = req.body;

  try {
    const openai = new OpenAI({
      apiKey: process.env.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,
    });

    const completion = await openai.chat.completions.create({
      model: "gpt-4",
      messages: [
        {
          role: "system",
          content: "You are an expert at analyzing business websites. Extract key information about the company's business model, team size, and potential VA needs."
        },
        {
          role: "user",
          content: `Analyze this website: ${website}`
        }
      ]
    });

    return res.status(200).json({ analysis: completion.choices[0].message.content });
  } catch (error) {
    console.error('Error:', error);
    return res.status(500).json({ error: 'Failed to analyze website' });
  }
}v
